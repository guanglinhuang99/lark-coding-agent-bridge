# 26ccdb2 双渠道受控原生验收交接

日期：2026-09-11。本轮成功检查时间：07:00:57—07:01:22 UTC。

## 当前结论

**PARTIAL_PREFLIGHT_PASS / NATIVE_ACCEPTANCE_NOT_RUN / SERVICES_UNCHANGED**。

用户在上一轮明确说明受控验收范围之后回复“下一步”。本轮按该范围继续：核对配置、空闲与回退条件，逐个渠道临时验证后恢复原版本；不合并 PR，不正式上线。不能把这次继续操作解释为允许降低权限、绕过工具限制、扩大业务数据范围或留下候选长期运行。

候选资产与现有企业微信即时健康检查已成功。随后包含任务账本状态、内网探测、用户客户端能力及接口阅读的预检批次，被工具安全层以“无法确定请求的安全状态”拦截，未返回执行结果。该批不能记为已执行或失败的业务测试。本轮没有通过其他命令或工具绕过拦截，没有停止任何服务。后续只继续了独立的项目源码阅读和本交接文档编写。

这是执行工具边界，不是测算业务失败，也不是新增代码回归。剩余工作是受控原生验收，不要重新开始共享架构重构。

## 固定对象

| 项目 | 已核验记录 |
| --- | --- |
| 仓库 | /Users/guanglin/Sync/wecom-bot |
| 分支 | refactor/shared-business-core |
| 源码提交 | 26ccdb2b01b4f7a9ad76922c5ccc9568148f6329 |
| PR | guanglinhuang99/lark-coding-agent-bridge #20，目标 main |
| 本轮查询的 PR 状态 | OPEN / MERGEABLE / CLEAN；本轮未重新查询全部 CI 日志 |
| 候选目录 | /Users/guanglin/.lark-channel/releases/shared-business-26ccdb2-Rx0yuR |
| 候选清单 | 上述目录的 candidate.json |
| 本轮完整性检查 | 清单内 12 项文件 SHA-256 全部一致，mismatches=[] |
| 历史验收说明 | docs/SHARED_BUSINESS_CANDIDATE_26CCDB2_20260911.md |
| 架构与配置约定 | docs/shared-business-core.md |

候选仍使用已有 node_modules 链接，不内置外部业务后端和独立 Python 环境。包版本字段 0.8.5 不能单独识别此次重构，应以完整提交和文件哈希为准。不要更改 candidate.json 中的未验收状态以伪造验收结果。

关键资产：

| 文件 | SHA-256 |
| --- | --- |
| dist/cli.js | 948a2661919bd48a7dd1ecdfa75200de56ee47cbd911bcb6fe6ef5efda18a216 |
| dist/wecom.js | 42a2247ffb47212a9d514e4c7d69cc9330d737a02abc9c85d58297b131715a2e |
| dist/risk/direct_bridge.py | 0c87e1bf23d9cf4504a6dc2650beb40db77445611a4d38fbbc1432fd636645bc |

## 本轮新取得的运行事实

| 检查 | 飞书 | 企业微信 |
| --- | --- | --- |
| 服务标签 | ai.lark-channel-bridge.bot.codex | ai.wecom-channel-bridge.riskbot-codex |
| 检查时 PID | 36940 | 77479 |
| launchd 状态 | running | running |
| 当前服务定义 SHA-256 | f1b6ebd5b11973bce0984bbbac4971700c2f9672b059e94849ccf2f9c8706608 | 4f21251b3f31086fa66fd302e6c14710c489cb9fccb563f4a9707997860b4985 |
| 平台健康 | 本轮未完成连接级验证 | connected，健康记录距检查约 1.8 秒 |
| 执行任务 | 尚未完成队列验证；检查时未见直接子进程 | activeRuns=0、startingRuns=0；队列账本尚未验证 |
| 业务 Python | 本轮未验证 | ready，PID 77480，sharedTextMemoization=optimized |

上述仅是检查时事实，不应作为之后停止服务的免复查依据。activeRuns=0 不证明没有排队、待确认或其他未落盘任务。

企业微信当前可复用的实际业务路径：

- Python：/Users/guanglin/.lark-channel/releases/wecom-risk-20260908-40e196ce3872/python-runtime/bin/python
- 外部后端：/Users/guanglin/Sync/risk-service
- 生产状态：/Users/guanglin/.lark-channel/releases/wecom-risk-20260908-40e196ce3872/state
- 受控配置引用：/Users/guanglin/Sync/wecom-bot/.env

这些路径本轮均确认存在；路径存在不等于内网、数据库与所有运行依赖已可用。未将 .env 的秘密内容输出或复制到候选包。

飞书 codex profile 的实际配置：personal、bot-only，allowedUsers / allowedChats / admins 均为 0 项。源码允许动态识别的机器人所有者使用个人模式，因此“显式允许名单为空”不等于所有者没有聊天权限。bot-only 是 CLI 身份设置，不能据此假定已有代表用户发送消息的 OAuth 权限，也不妨碍用户在客户端手工测试。

企业微信现有 ProgramArguments 包含当前入口之后的一个历史脚本参数。回退时必须恢复实际原始定义，不应顺手删除或修正历史参数；候选启动参数是否需要变化应单独核对。

## 本机 Codex 的下一步

### 1. 只补剩余前置检查

先读取本文件、架构约定和适用 AGENTS.md。确认 Git HEAD、PR HEAD 与候选清单一致，核对已有资产哈希，不要在生产工作区重新 build。不重复已通过的全量 CI；只有新增改动、失败或明确未覆盖风险才扩大测试。

在常规授权范围内检查两渠道实际运行及排队任务、平台连接、业务内网可达性，以及用于收发测试消息的真实用户会话。任何权限、工具或网络检查被拒绝时，保留现有服务，记录阻塞，不关闭沙箱或审批、不修改代理与路由绕过、不伪造客户端入站消息。若必须由用户在客户端发送，只请求一次必要的客户端操作，不能用 bot 自发消息冒充用户入站验收。

### 2. 准备可恢复的单实例维护

先在受控本机位置保留两个服务的原始启动定义与其哈希，记录已有配置引用和恢复方法，避免在公开日志输出完整环境。保护原有未跟踪文件，禁止 reset、clean、force-push、覆盖无关改动、删除锁文件或重置生产账本。

准备飞书候选的临时公共 RISK_* 配置，复用已核验的 Python 与后端路径；独立记录测试状态目录。飞书风险允许用户只能是明确核验的发起用户，不借用企业微信 user ID，不设通配允许或关闭检查。无法确认该用户时保留锁定并记录所缺身份。

两个渠道均明确核验最终生效的 RISK_BRIDGE_PATH 指向本候选的 dist/risk/direct_bridge.py，防止仍继承旧 WECOM_RISK_BRIDGE_PATH 而形成新 JS 配旧桥接脚本。配置仅用于本轮候选进程，默认不持久化修改生产 plist 或 .env。

保持现有会话、工作区和历史账本完整，避免复制整个生产配置或缓存到测试包；需要新状态目录时必须注明，不可把冷缓存差异当作性能回归。

### 3. 逐个渠道验收，结束后恢复

所有必要检查通过且原服务无运行/排队任务后，逐个渠道暂停原连接并等待其拥有的业务子进程退出，再启动该渠道的唯一候选实例；另一渠道保持原样。不要用 start --all 代替受控切换，不同时运行相同机器人身份的生产与候选连接。

每个渠道核对实际入口、PID、桥接脚本路径和哈希、权限、平台连接以及业务 ready，再在已确认的私聊或受控会话内测试。复用用户已批准的单账户/主体样本；没有已确定的样本则只做用法与权限等非数据测试，并记录业务矩阵尚未执行，不自行扩大账户或数据范围。

| 最小验证项 | 通过标准 |
| --- | --- |
| /测算、/授信 用法 | 由共同业务入口响应，不进入普通 Agent |
| 单主体授信及一个账户限额查询 | 当前后端数据，口径一致，失败不当作零 |
| 一次标准测算及明确确认 | 摘要与后端参数一致；确认前不测算，重复确认不重复调用 |
| 修改金额及旧确认 | 必须核对新摘要；旧稿不能执行新参数 |
| 一次取消 | 未开始的任务不继续，迟到结果不被展示为正常成功 |
| 展示 | 企业微信卡片、飞书文本选择可操作；长结果最后一行可核对 |

难以安全在真实客户端制造的延迟、旧回调、跨用户和断线重投场景，可引用现有对应模拟回归，但必须标明模拟，不能写成原生通过。不查询更多金融样本来重复证明已有代码合同。

无论成功或失败，完成本渠道测试后都关闭本轮候选及其子进程，按冻结原定义恢复原服务并确认平台连接，然后再处理另一渠道。不得把测试候选自动留作生产版本，不用旧账本快照覆盖当前状态。恢复失败应立即报告，不继续切换另一渠道。

## 交付和终态要求

新增实际验收结果文件 docs/SHARED_BUSINESS_NATIVE_RESULTS_26CCDB2_20260911.md。各项注明 PASS / FAIL / NOT RUN，写明实际提交、入口哈希、测试消息标识、后端调用是否发生、恢复后的 PID 与健康。敏感原始结果只留在受控本机位置，不进入 Git、PR 或聊天回复。

最终分别报告：代码/候选验证、真实客户端收发、真实业务数据、原服务恢复。不可互相替代。本阶段不提交批准、不合并 PR、不打 tag、不发布 npm、不正式上线；需要下一阶段时只提出尚缺的具体动作，不再重复重构和候选构建。

## 本轮执行证据

- 代码与服务状态：op_9e837a8e7132721b7619f010 的 git-service 成功步骤，退出码 0。
- 候选及服务脱敏预检：task_baf23c28576f4ff3，退出码 0；op_8eb765cef9a6b9e04bb3499c 的 preflight 成功步骤。
- 后续预检批次：平台拦截，无执行 task ID；不能记作已执行。
- 本轮没有发送真实消息，没有测算真实账户，没有修改或重启生产服务。
