# 55778ed 终态修复：双渠道原生复验交接

## 对象与当前边界

仓库：`/Users/guanglin/Sync/wecom-bot`。
分支：`refactor/shared-business-core`。
本地修复提交：`55778edff0e1680c03dcddcf4078644316d591fd`。
候选目录：`/Users/guanglin/.lark-channel/releases/shared-business-55778ed-5AUNWn`。
候选清单：目录内 `candidate.json`；源码 tree 为 `709121f9b9675ca5c140f54a072a88a8627afea3`。

本文件只交接新候选的原生复验；它不是原生通过记录或生产上线授权。不要复用旧 `26ccdb2` 候选冒充修复后版本，不需要重新开始重构或重复打包。保持原生失败报告 `SHARED_BUSINESS_NATIVE_RESULTS_26CCDB2_20260911.md` 原样。

核心哈希：

| 资产 | SHA-256 |
| --- | --- |
| dist/cli.js | a11d2c0fe8da06c04c2fde8b741413f597ac2146f7346db179e513e15401ee10 |
| dist/wecom.js | ea0e96c930ce8678f50a86f75ca550c46ae96167b035469125d85ba7b4238dcd |
| dist/risk/direct_bridge.py | 0c87e1bf23d9cf4504a6dc2650beb40db77445611a4d38fbbc1432fd636645bc |

12 项清单资产已经复核；两个入口来自同一提交和同一次构建。包版本字段仍为 0.8.5，不能据此判断是否已加载终态修复。依赖链接到现有 node_modules；外部 risk-service、Python 和凭证不在包内。

## 本轮验证与复审限制

2026-09-11，本地提交已完成：11 个相关文件，包含先前终态修复和本轮停止/回执顺序修正，合计新增 27 项回归（此前 25 项、本轮 2 项）。本轮先复现了两渠道回执发送挂起阻塞普通停止的问题，再修正顺序。

固定提交在独立工作树、独立 HOME 中执行：172 个测试文件通过、1 个跳过；1345 项测试通过、1 项跳过、零失败。跳过的是可选性能基准。TypeScript 类型检查通过。前端、服务端构建、三个 JavaScript 入口语法、依赖解析、12 项资产清单及 Python 脚本一致性/AST 均通过。无生产配置的飞书 help 正常，企业微信 health 按预期返回 missing 并拒绝就绪。

独立 Codex 增量复审没有完成：固定提交的审查进程在 180 秒时限后结束，没有输出最终结论。此前另一项审查也因运行时 45 秒上限终止。两者都不得写为审查通过，亦没有足够证据宣称审查发现了新的缺陷。需要对本固定提交补齐独立复审后，再进行原生复验；不要再次重复已完成的全量测试或打包，除非产生新修改。

09:00:13 UTC 的只读收尾查询：本地 HEAD 为 55778ed；PR #20 仍为 26ccdb2、OPEN。本轮没有推送或触发新 CI，旧 PR 绿色检查不代表新提交。生产飞书 PID 58157、企业微信 PID 57364，均 runs=1；本轮没有重启、修改真实配置或连接平台。所有原生复验及真实 RPC 计数仍为 NOT RUN。

执行证据：构建 task_e62e6c927108f109；隔离入口与哈希 task_a0f345852372e087；全量测试及类型 task_17a4b6b040223be4；未完成的复审 task_ece11ff58833bfe0。本交接为本地未提交文档，不改变候选源代码和哈希。

## 只复验终态关键矩阵

沿用已经核验的私聊、发起用户、账户、标的和受控验收范围。每个渠道依次执行以下矩阵，不扩大金融样本范围，不下单、不修改生产业务数据。

| 步骤 | 输入与期望 | 风险测算调用增量 | 普通 Agent 启动增量 |
| --- | --- | --- | --- |
| A | 重建与前次相同的 100 万草稿，再改为 200 万；仅展示新摘要 | 0 | 0 |
| B | 明确确认新摘要，收到结果后等待本轮调用终态 | 1 | 0 |
| C | 在 5 分钟内再发“确认”；回复“该确认已处理/不会重复执行测算” | 0 | 0 |
| D | 新建一个待确认草稿后 `/stop`；必须出现“已取消风险交互” | 0 | 0 |
| E | 在 5 分钟内再发“确认”；回复“该风险交互已取消”，不进入普通聊天 | 0 | 0 |
| F | 再次发起新草稿并确认，证明结束标记不永久锁住业务 | 1 | 0 |
| G | 发起不涉及金融数据、文件写入或工具操作的普通问题，再进行普通聊天确认 | 0 | 记录实际普通对话启动次数 |

新标记默认保留 5 分钟、最多 2000 项，只保留结束原因和截止时间；普通新问题会移交给普通 Agent。超过 TTL、进程重启或容量淘汰不属于本次短期终态提示保证，不能把这些情况误报为本补丁承诺的永久保护。

企业微信旧卡片回调、延迟、跨用户、断线及重投等前次 NOT RUN 项仍单独标注；没有安全、无歧义的测试条件就保留 NOT RUN，不能用上述矩阵或模拟测试替代。

## 计数证据

客户端“不再显示第二份结果”不足以证明零调用。开始每项测试前记录候选 PID、时间窗口和测试消息 ID，完成后记录窗口终点及零运行/排队状态。

现有公共运行时在 `risk-runtime` 的 `call` 事件中输出 method、outcome、durationMs；可用 `method=calculate_pretrade` 统计完成事件作为辅助证据，但它不等于每条 RPC 被 Python 接收的精确证明。不得把 PQ 缓存行数当作 RPC 次数。

精确 RPC 计数需要在本次候选拥有的 JS→Python 请求边界记录成功写出的请求 ID/方法，并在相应子进程响应边界核对同一 ID 的终态。只保存 ID、方法、时间、结果状态；不保存 args、产品、交易金额、令牌或金融结果正文。普通 Agent 统计应核对实际进程/执行器启动事件并区分 risk-intent 解析器与普通对话。

优先使用已有可验证日志；如需临时观测，仅在受控候选进程使用可撤销、只记录元数据的观测方式，不修改生产业务后端或覆盖候选资产。观测会改变构建文件时必须另立版本/哈希，不能仍称为原 55778ed 包。没有可核验计数时明确写 NOT RUN，不为获得计数放宽权限或伪造输入。

## 单实例维护与恢复

先读 `SHARED_BUSINESS_NATIVE_HANDOFF_26CCDB2_20260911.md` 和原生结果，复用已批准的逐渠道临时验证并恢复流程。重新核对当前任务/队列、连接、后端可达性、配置引用和原始 plist 字节哈希、权限；历史 PID 只供比对，不是直接结束进程的依据。

已恢复的服务：飞书 `ai.lark-channel-bridge.bot.codex`；企业微信 `ai.wecom-channel-bridge.riskbot-codex`。本轮提交前只读核查时 PID 分别为 58157 和 57364。

每次只替换一个渠道。原连接及其业务子进程退出后再启动唯一候选实例，另一渠道保持不变。只沿用已核验的用户允许名单；使用新独立测试状态目录、当前候选 Python 桥接脚本及已配置业务后端。不要在原服务运行时启动同身份第二连接，不用 start --all 作为替换捷径。

每个渠道测试无论通过或失败，都停止本轮候选并按原字节/权限恢复原服务，核对连接、任务状态及无候选残留。恢复失败时停止后续渠道切换并立即报告。

## 结果记录

新增 `docs/SHARED_BUSINESS_TERMINAL_NATIVE_RESULTS_55778ED_20260911.md`。每项写 PASS / FAIL / NOT RUN，并分别记录真实客户端回复、实际 Agent/RPC 计数、后端是否调用、恢复结果。没有本轮证据的项目不能写通过。

本阶段不合并 PR、不打 tag、不发布 npm、不将候选留作生产，不修改或重写历史原生报告。提交、推送和上线是不同动作；先核对本地/PR HEAD，不假定本地提交已更新远端。
