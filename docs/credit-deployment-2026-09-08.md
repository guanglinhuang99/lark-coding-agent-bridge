# /授信 上线检查（2026-09-08）

最新状态：已于 20:49 完成上线，PID 62519，connected=true。以下保留此前检查过程。

- 当前目标：`ai.wecom-channel-bridge.riskbot-codex`。
- 当前发布：`wecom-account-20260908-1733dab0256f`。
- 候选发布：`/Users/guanglin/.lark-channel/releases/wecom-credit-20260908-078624e8c14e`。
- 候选 SHA256：`078624e8c14efe0987d4b432051aef012d45bc8bd7b851d459ae1774285e134a`。
- 制品基于当前线上版本，只加入授信命令、独立表格模块、批量客户端方法和授信分发。保留线上账户匹配修复及其他原制品内容，不打包当前工作区其他新增改动。
- Python bridge 只新增 `get_credits` 分发；使用既有风险服务 `build_credit_reports`。
- 已保存 `original.plist` 和 `candidate.plist`，保留原 Python 环境与会话状态路径。

验证：39 文件 334 项 WeCom 测试通过，TypeScript 检查通过。候选制品语法及实际制品提取的命令、权限、待确认交易保留、50 主体分段检查通过。

真实只读检查：使用 LaunchAgent 的 Python、环境和服务目录，桥接 ready/ping 成功；宁德时代及赣锋锂业批量查询在 PQ SQL API 读取超时。三次重试总耗时约 103 秒。当天精确日期查询和最小 `SELECT 1 AS X FROM DUAL` 查询均在约 102 秒重试后超时；内网服务器根路径可正常返回 HTTP 307。尚未完成真实授信结果与企业微信端实测，未发送测试消息。

缺少 FastAPI/FastMCP 只影响完整服务测试，并不阻止当前直接桥接启动；本次真实阻塞为 SQL API 响应超时。

20:39 重试：桥接 ready/ping 成功，真实批量授信查询仍遇到 pqread SQL API 30 秒读取超时，三次重试共约 103.24 秒。保持原发布，未切换或重启。

## 原因纠正与最终上线

对照任务 `01a08100-cd14-7302-b19e-1668de59cbc2` 的成功 SQL 后，两个 Python 解释器及完整桥接环境均成功查询净值表；桥接环境约 1.34 秒，请求未走代理。最小 SELECT 1 随后约 0.17 秒返回，按 2026-09-07 日期读取持仓日期约 0.48 秒返回。授信新增的全持仓视图 MAX 日期查询则超过 8 秒读取超时。此前将查询超时概括为 PQ 整体不可用不充分；此前其他查询超时是否受慢查询排队影响未确证。

修正日期发现：从今天起用等值日期查询依次向前查找，最近七天无记录时才回退到更早日期的聚合。保留最新日期、当天数据和缺失数据语义。10 项授信 Python 测试通过，源文件和打包文件同步。

真实批量查询成功：宁德时代、赣锋锂业共 2 主体，持仓日期 2026-09-07，无 unmatched/errors，耗时 38.84 秒；剩余额度与授信/占用金额算术一致。

20:49:19 切换到 `wecom-credit-20260908-078624e8c14e`；旧 PID 46026 退出，新 PID 62519 重新连接成功，风险快路径启用，activeRuns=0、startingRuns=0。配置和会话路径保留；未发送企业微信测试消息。原配置保存在发布目录 original.plist。
