# 终态修复：独立审查与处置

日期：2026-09-11。审查基线为 `55778edff0e1680c03dcddcf4078644316d591fd`，本文件不表示原生复验通过。

## 已完成的独立审查

独立 Codex 只读审查成功返回最终结论，任务 `task_a5973446b4cc8adc`，退出码 0。审查范围为该提交的四个生产实现文件、必要渠道调用者及终态/停止测试，不运行构建或测试，不读取生产配置。与前两次超时不同，本次已取得完整结论。

| 审查意见 | 核验与处置 |
| --- | --- |
| 飞书取消通知永久挂起会阻塞关闭 | 有效。新增模拟回归实际失败；复用已有 withTimeout，退出只对通知等待最多 1000ms，并拒绝关闭后的新发送。已经提交的平台请求不保证物理取消。 |
| /new 仍保留上一轮风险交互归属 | 有效。两个渠道在待确认、已处理、已取消三种状态共六项回归实际失败。新增共享 reset/resetScope，清除本范围活动状态与轻量终态；/stop 仍保留终态提示。 |
| 飞书普通 /stop 会重复发送空闲回执 | 不成立。src/commands/index.ts 的 handleStop 仅在提供 targetScope 时发送回执；不带参数时明确不回复。正式 startChannel 集成测试增加单条回执精确断言并通过；没有为了该意见改变原停止语义。 |
| 企业微信取消通知挂起使入站请求不结束 | 有效。模拟回归实际失败，尽管活跃任务已经停止。使用共享 withTimeout 对取消通知有限等待 1000ms，记录失败但不重发业务。 |

另由本轮代码核验发现：RiskArrival 只约束活动草稿，未约束轻量终态。早到普通消息在异步聊天识别后捕获了较新的结束标记，会清除此标记。新增回归实际失败后修复：终态内部保存公共 revision，捕获时按同一 maxRevision 过滤。提示内容仍只有结束原因与截止时间，不保存金融数据。

## 复现证据

首批关闭/终态版本测试：两项失败，09:24:43 UTC，命令退出码 1。首批修复后包含原确认与停止回归的 143 项测试、类型及差异检查通过，任务 `task_3c39fcfbe47a33de`。

第二批 /new 与企业微信回执挂起测试：七项失败；两项飞书正式入口终态测试通过，其中取消响应只有一条。相关修复加入共享 reset API、复用已有 withTimeout，并接入企业微信文本及两个新会话卡片入口。

最终相关验证于 09:29:34 UTC 完成：76 个文件、681 项通过、零失败零跳过，随后 TypeScript 类型检查和 Git 差异检查通过；任务 `task_8f55b7a1205fc287`。本轮新增九项回归并加强飞书取消单回执断言，没有重跑整个仓库全量 CI。原生界面预检只确认两个应用已打开、原服务 PID 58157/57364 仍运行，尚未发送消息。

修复后独立复审与新候选标识须以本轮实际返回结果为准。旧 55778ed 候选不包含新增修复，不能拿它当作这些缺陷已修复的原生验收对象。

## 原生与发布边界

此阶段不合并、不推送、不正式上线。原报告 `SHARED_BUSINESS_NATIVE_RESULTS_26CCDB2_20260911.md` 保持原样。用户已同意的受控复验仍须满足单实例、限定会话、原定义恢复及准确计数；没有新证据时原生项目保留 NOT RUN。
